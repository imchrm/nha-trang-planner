# TODO.md — Список задач

> Статус: `[ ]` не сделано · `[x]` готово · `[-]` отменено / не актуально

---

## Срочно / Перед финальным запуском

- [ ] Убрать DebugPanel из продакшена — завернуть в `import.meta.env.DEV`:
  ```jsx
  {import.meta.env.DEV && (
    <DebugPanel ... />
  )}
  ```
- [ ] Проверить QR-код и «Скопировать ссылку» на живом URL (https://360tur.uz/tours/nha_trang_planner/)
- [ ] Проверить сценарий LoadConflictModal — открыть ссылку с `#s=` на устройстве с уже сохранённым расписанием
- [ ] Протестировать на iPhone Safari (особенно localStorage и env(safe-area-inset-bottom))

---

## Улучшения UX

- [ ] **PWA-манифест** — добавить `manifest.json` и `<link rel="manifest">` чтобы работало «Добавить на главный экран» на iOS и Android
- [ ] **Печатная версия** — `@media print` CSS: скрыть каталог, показать только расписание, убрать кнопки
- [ ] **Пустое состояние сводки** — когда ни один день не заполнен, показывать подсказку «Начните с выбора активностей выше»
- [ ] **Анимация добавления карточки** — плавное появление PlacedCardChip в слоте
- [ ] **Счётчик оставшихся карточек** на табах категорий (показывать сколько ещё не размещено)

---

## Технический долг

- [ ] Разбить App.jsx на модули при достижении ~3000 строк:
  - `src/data/cards.js` — массив CARDS
  - `src/data/days.js` — массив DAYS + TRIP
  - `src/constants/theme.js` — THEME, CAT
  - `src/utils/persistence.js` — encode/decode/localStorage
  - `src/components/` — по компоненту в файл
- [ ] Добавить PropTypes или TypeScript для компонентов
- [ ] Вынести `intensityColor()` и `extractCostNumber()` в `src/utils/helpers.js`
- [ ] Покрыть `canPlaceCardOnDay()` юнит-тестами (Vitest) — критичная логика

---

## Функциональные идеи (обсудить)

- [ ] **Заметки к дню** — небольшое текстовое поле под карточками дня для пометок (время встречи, адрес, телефон гида)
- [ ] **Сравнение вариантов** — возможность сохранить «Вариант A» и «Вариант B» и переключаться
- [ ] **Итоговый список** — кнопка «Сформировать список» генерирует текст в виде дней с активностями для отправки в Telegram
- [ ] **Drag & drop на тач-экранах** — сейчас drag отключён на мобильном; реализовать через touch events или библиотеку `@dnd-kit/core`

---

## Готово

- [x] Базовый конструктор: каталог карточек + шкала дней
- [x] Система весов (4 единицы на день, комбинации длительностей)
- [x] Двухдневная карточка (А-5 Далат, weight=8)
- [x] Drag & drop на десктопе (HTML5 API)
- [x] Тап-взаимодействие: CardPickerModal и DayPickerModal
- [x] Date-adaptive фазы (preparation / active / birthday / memories)
- [x] Birthday-режим: конфетти, баннер, коралловый фон, праздничный слот
- [x] DebugPanel с пресетами дат
- [x] DurationIndicator (квадратики на карточке и в слоте дня)
- [x] IconRating (звёзды + смайлики вместо цифровых оценок)
- [x] IntensityChart (гистограмма ритма нагрузки с предупреждением)
- [x] Summary (сводка: заполненность, бюджет, темп, впечатления)
- [x] BlockingAlertModal (конкретные даты при ошибке 2-дневной карточки)
- [x] ResetConfirmModal (подтверждение сброса, без window.confirm)
- [x] localStorage авто-сохранение
- [x] URL-шаринг (#s=base64)
- [x] ShareModal (QR + ссылка + JSON экспорт/импорт)
- [x] LoadConflictModal (Вариант Б при конфликте URL vs localStorage)
- [x] UrlLoadedBanner (тост после загрузки из ссылки)
- [x] Адаптивность: wide (карточки сверху) / narrow (табы снизу)
- [x] Vite-деплой в подпапку (base: '/tours/nha_trang_planner/')
- [x] Инструкция для пользователей
